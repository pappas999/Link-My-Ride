import { createMachine } from "xstate"

export const addVehicleFormMachine = createMachine({
    id: "addVehicleForm",
    initial: "initial",
    context: {
    },
    states: {
        initial: {

        }
    }
})